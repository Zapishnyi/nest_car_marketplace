export declare class TransformHelper {
    static trim({ value }: {
        value: string;
    }): string;
    static toLowerCase({ value }: {
        value: string;
    }): string;
    static toUpperCase({ value }: {
        value: string;
    }): string;
    static toNumber({ value }: {
        value: string;
    }): number;
    static toLowerCaseArray({ value }: {
        value: string[];
    }): string[];
    static toUpperCaseArray({ value }: {
        value: string[];
    }): string[];
    static uniqueItemsArray({ value }: {
        value: string[];
    }): string[];
    static trimArray({ value }: {
        value: string[];
    }): string[];
    static toBoolean({ value }: {
        value: string;
    }): boolean;
}
